{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edycja kwiatu</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edycja" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('flowers_index') }}">Powrot</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Usun">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
